# Twenty CRM API key (create at Settings → API keys in Twenty)
TWENTY_API_KEY=your-twenty-api-key-here

# Optional: shared secret for webhook auth (set same value in Earshot webhook URL as Bearer token)
BRIDGE_SECRET=
